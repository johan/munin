#!/bin/sh
[ -x @@LIBDIR@@/munin-update ] && @@LIBDIR@@/munin-update $@;
[ -x @@LIBDIR@@/munin-limits ] && @@LIBDIR@@/munin-limits $@;
[ -x @@LIBDIR@@/munin-graph  ] && nice @@LIBDIR@@/munin-graph --cron $@ | while read line; do [ x"$line" = x"*** attempt to put segment in horiz list twice" ] && continue; echo $line; done;
[ -x @@LIBDIR@@/munin-html   ] && nice @@LIBDIR@@/munin-html $@;

